import "@radix-ui/themes/styles.css";

// TODO: add to README that consumers need to wrap the app with ThemeProvider

export { Theme as ThemeProvider } from "@radix-ui/themes";
export { Table } from "./components/Table/Table";
